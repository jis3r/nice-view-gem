if(CONFIG_NICE_VIEW_GEM_PREVIEW AND CONFIG_ZMK_DISPLAY AND CONFIG_NICE_VIEW_WIDGET_STATUS)
  set(NICE_VIEW_GEM_DIR ${CMAKE_CURRENT_LIST_DIR}/../nice_view_gem)

  zephyr_library_include_directories(${CMAKE_SOURCE_DIR}/include)
  zephyr_library_sources(preview_mock.c)
  zephyr_library_sources(preview_status_screen.c)
  zephyr_library_sources(${NICE_VIEW_GEM_DIR}/assets/images.c)
  zephyr_library_sources(${NICE_VIEW_GEM_DIR}/assets/crystal.c)
  zephyr_library_sources(${NICE_VIEW_GEM_DIR}/widgets/animation.c)
  zephyr_library_sources(${NICE_VIEW_GEM_DIR}/widgets/battery.c)
  zephyr_library_sources(${NICE_VIEW_GEM_DIR}/widgets/claude_stats.c)
  zephyr_library_sources(${NICE_VIEW_GEM_DIR}/widgets/output.c)
  zephyr_library_sources(${NICE_VIEW_GEM_DIR}/widgets/screen_peripheral.c)
  zephyr_library_sources(${NICE_VIEW_GEM_DIR}/widgets/util.c)
endif()
